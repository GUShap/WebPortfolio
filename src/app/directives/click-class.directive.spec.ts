import { ClickClassDirective } from './click-class.directive';

describe('ClickClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickClassDirective();
    expect(directive).toBeTruthy();
  });
});
